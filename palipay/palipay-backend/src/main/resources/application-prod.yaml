spring:
  application:
    name: palipay-backend

  datasource:
    url: jdbc:mysql://127.0.0.1:23305/palipay?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1q2w3e4r!@
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000

  data:
    redis:
      host: 43.202.40.75
      port: 26379
      timeout: 6000
      lettuce:
        pool:
          max-active: 10
          max-idle: 5
          min-idle: 2
          max-wait: -1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  sql:
    init:
      mode: never
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${PALIPAY_ADMIN_EMAIL}
    password: ${PALIPAY_ADMIN_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true